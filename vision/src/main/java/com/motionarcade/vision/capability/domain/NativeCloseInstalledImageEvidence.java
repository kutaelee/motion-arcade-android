package com.motionarcade.vision.capability.domain;

/**
 * Deliberately empty non-authority boundary marker.
 *
 * Installed-APK parsing produces diagnostic inspection material only. There is no Plan,
 * InstalledImages evidence contract, evidence factory, or native-create authorization bridge in
 * this package. A future Android issuer must open, snapshot, parse, and consume PackageManager-
 * paired held FDs entirely inside its own non-caller-input authority boundary.
 */
final class NativeCloseInstalledImageNonAuthorityBoundary {
    private NativeCloseInstalledImageNonAuthorityBoundary() {}
}
